# HolonBridge

LLM ↔ Jena 6.0 bridge service.  Exposes a Fuseki triplestore to an LLM agent
through natural-language query, SHACL-validated update, and capability discovery.

Runs as a sidecar alongside HolonViewer (port 3031 vs 3000).

---

## Architecture